package RenansTech.Strings;

import javax.sound.midi.Soundbank;
import java.util.Scanner;

public class Scan_er {
    public static void main(String[] args) {
        /* scanner is a library under java
         this library has ready methods that you can use it
         to be able to use this library we need to call them in our class
         and create object from that library
         The char doesn't work in Scanner because its covered
         */

//         .next (); read the string    .nextInt (); reads an integer   .nextDouble (); reads the double
//         .nextByte (); reads the byte  .nextShort - .nextLong   . nextFloat   . nextBoolean();
//         when one of these methods is called, the program will  pause  execution and wait for the user to enter a value, the entered value will be returned by these methods;

        //Scanner input = new Scanner (System.in);
        // System.out.println(input.next());
        //System.out.print("Enter Your Name:");
        //System.out.println("Print your name is " +input.next());
        //System.out.println("Print your name is " +input.nextLine());   > if you do this is will read after the space as well, so you can read whole name



        //  Scanner scan = new Scanner ( System.in);
        //System.out.println("what's your name:");
        // byte bytenumber = scan.nextByte();
        // System.out.println("now my byte number is "+bytenumber);

        // now my byte number is == 50


       // Scanner number = new Scanner(System.in);
       // System.out.println("Enter int number::: ");
       // int num = number.nextInt();
       // System.out.println("nummm");

    }
}
